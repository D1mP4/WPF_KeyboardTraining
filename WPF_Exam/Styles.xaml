<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <ControlTemplate x:Key="BT" TargetType="{x:Type Button}">
        <Grid>
            <Button Foreground="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type Button}"  x:Key="BStyle1">
        <Setter Property="Template" Value="{StaticResource BT}"/>
        <Setter Property="Background"  Value="Cyan"/>
        <Setter Property="FontSize" Value="30"/>
        <Setter Property="FontFamily" Value="Times New Roman"/>
        <Setter Property="Margin" Value="2,2,2,2"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="Width" Value="45"/>
    </Style>

    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource BStyle1}" x:Key="BStyle2">
        <Setter Property="Background" Value="LightCoral"/>
        <Setter Property="FontFamily" Value="Courier New"/>
        <Setter Property="FontSize" Value="20"/>
    </Style>

</ResourceDictionary>